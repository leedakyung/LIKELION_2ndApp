<h1>모든 노트들 목록입니다.</h1>
<h2><a href = '/notes/new'>New note</a></h2>
<ul>
  <% @notes.each do |note| %>  <!--%옆에 = 기호 있으면 다 ~ 보여줌 -->
      <li><a href = '/notes/<%= note.id %>'><%= note.title %></a></li>
  <% end %>
</ul>